# ========== 站点核心 ==========
baseURL: "https://emofog.github.io/blog/"  # 站点最终部署的完整 URL，末尾建议保留 /
languageCode: "zh"                         # 站点默认语言编码，zh 代表中文
title: "全麦面包"                          # 浏览器标签 & SEO 标题
theme: "PaperMod"                          # 当前使用的主题目录名（themes/PaperMod）

# ========== 构建控制 ==========
enableInlineShortcodes: true  # 允许在 Markdown 中直接写内联短代码，如 {{< ref >}}
enableRobotsTXT: true         # 自动生成 robots.txt，利于搜索引擎
buildDrafts: false            # 生产环境不编译草稿（draft: true 的文章）
buildFuture: false            # 不编译发布日期在未来的文章
buildExpired: false           # 不编译已过期的文章
enableEmoji: true             # 支持 GitHub 风格的 Emoji，如 :smile:
pygmentsUseClasses: true      # 代码高亮使用 CSS 类名，方便自定义主题色
mainsections: ["posts"]       # 首页“最近文章”只扫描 content/posts 目录

# ========== 性能优化 ==========
minify:
  disableXML: true  # 不压缩 XML（如 RSS），避免 PaperMod 某些模板出错

# ========== 分页 ==========
pagination:
  pagerSize: 5  # 列表页每页 5 篇文章，超过则自动分页

# ========== 多语言（仅保留中文） ==========
languages:
  zh:
    languageName: "中文"  # 语言选择器里显示的文字
    weight: 1             # 权重越小越优先
    title: "云在青山月在天"     # 中文站标题，可覆盖全局
    taxonomies:           # 启用三大分类系统
      category: categories
      tag: tags
      series: series
    menu:                 # 顶部导航栏（顺序按 weight 升序）
      main:
        - name: 🏠 首页
          url: /
          weight: 1
        - name: 📦 归档
          url: /archives/
          weight: 2
        - name: 🏷️ 标签
          url: /tags/
          weight: 3
        - name: 🔍 搜索
          url: /search/
          weight: 4
        - name: ℹ️ 关于
          url: https://emofog.github.io/
          weight: 5

# ========== 主题参数（PaperMod 专用） ==========
params:
  env: production                    # 生产环境，启用 PWA、Analytics 等
  description: "一个简洁、快速、响应式的中文 Hugo 博客"  # SEO 默认描述
  author: "emofog"                   # 文章底部作者名
  defaultTheme: auto                 # 跟随系统亮/暗，可选 light / dark
  ShowShareButtons: false             # 文章底部显示分享按钮
  ShowReadingTime: true              # 预估阅读时长
  ShowPostNavLinks: true             # 文章底：上一篇/下一篇
  ShowBreadCrumbs: true              # 面包屑导航
  ShowCodeCopyButtons: true          # 代码块一键复制
  ShowToc: true                      # 自动生成目录（需要文章内 ## 标题）
  ShowRssButtonInSectionTermList: true  # 分类/标签页显示 RSS 图标
  ShowAllPagesInArchive: true        # 归档页一次性列出所有文章
  ShowPageNums: true                 # 底部分页数字
  displayFullLangName: false         # 语言选择器只显示“中文”而非“Chinese”

  # 首页自我介绍卡片
  homeInfoParams:
    Title: "全麦面包的博客"
    Content: >
      🍞 欢迎来到我的技术笔记与生活随笔！

  # 社交图标（支持 font-awesome 名称）
  socialIcons:
    - name: github
      url: "https://github.com/emofog"

  # “编辑此页” 链接，指向 GitHub 文件位置
  editPost:
    URL: "https://github.com/emofog/blog/tree/main/content"
    Text: "编辑此页"
    appendFilePath: true  # 自动把文章路径拼到 URL 后

  # 本地搜索（Fuse.js）调优
  fuseOpts:
    isCaseSensitive: false  # 不区分大小写
    shouldSort: true
    location: 0
    distance: 1000
    threshold: 0.4           # 匹配阈值，越小越严格
    minMatchCharLength: 2    # 至少输入 2 字符才触发搜索
    keys: ["title", "permalink", "summary", "content"]

  # 静态资源
  assets:
    disableHLJS: false  # false=启用 highlight.js 代码高亮

  analytics:
    bing:
      SiteVerificationTag: ""

# ==========  markup 解析器 ==========
markup:
  goldmark:
    renderer:
      unsafe: true  # 允许在 Markdown 中直接写 HTML（如居中、自定义样式）
  highlight:
    noClasses: false  # 使用 CSS 类高亮，配合 pygmentsUseClasses: true
    style: monokai    # 高亮配色，可选 github/monokai/dracula 等

# ========== 输出格式 ==========
outputs:
  home:
    - HTML
    - RSS
    - JSON  # 用于搜索索引或 PWA